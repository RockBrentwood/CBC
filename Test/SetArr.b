// Dynamic array testing

define showArr(x, y) {
   auto i;
   for (i = x; i < y; i++) <- a[i], " ";
"
"
}

define setArr(x, y) {
   auto i
   for (i = x; i < y; i++) a[i] = i
}

s = setArr(  0,  10), s = showArr(  0,  10);
s = setArr(500, 510), s = showArr(500, 510);
s = showArr(0, 10);
quit
